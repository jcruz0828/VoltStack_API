package com.example.job_tracker.enums;

public enum JobType {
    FULL_TIME,
    PART_TIME,
    CONTRACT,
    TEMPORARY,
    INTERN,
    FREELANCE,
    REMOTE,
    ON_SITE,
    HYBRID;
    // You can add additional methods or properties if needed
}
